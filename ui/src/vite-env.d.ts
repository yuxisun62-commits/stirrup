/// <reference types="vite/client" />

// Compile-time constants injected by vite.config.ts `define`. The
// runtime value gets string-substituted into the bundle at build time,
// so this declaration just tells TypeScript the identifier exists.
declare const __APP_VERSION__: string;
